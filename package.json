{
  "name": "sd-tailscale-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Stream Deck control surface for Tailscale (plugin + core CLI client)",
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev:plugin": "pnpm -F @sd-tailscale/plugin dev"
  }
}
